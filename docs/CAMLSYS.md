# CaMLSys deployment, 11 September 2026

**Completed:** job `55667` finished successfully in 9m49s (exit 0). E1 and E2 are complete for 40 advice and 40 math prompts, one seed, five measured directions. The artifact verifier passed: 354,533 token rows, six figures, all configured E2 transplants present. Results are copied locally to `runs/a40-two-hour-reserves/results.md`. Completion was about 45 minutes after the first allocation began, including the stopped attempt and recovery interval. Human review and sentence-judge annotation are still incomplete. The launch history below is retained for provenance.

**Current job: `55667`**, running the reserve extension in `runs/a40-two-hour-reserves`; progress is in `logs/reserves-55667.out` and `logs/reserves/stage-*.log`. Original job `55666` ended after 22m33s when the sample-size gate found 45 eligible advice pairs but only 36 math pairs. No E1 extraction or hypothesis result was produced in that attempt. The extension increases the fixed candidate cap from 50 to 100, reuses all 249 existing transcripts and the unchanged validated ruler, and generates only 100 new responses (50 additional math prompts × two conditions). It preserves the 40-pair minimum and all other settings. The original artifacts remain intact; `extension.json` records provenance and source hashes. A targeted extension test verified exact cached-token/ruler preservation and rejection of changed scientific settings. The resumed runner measures all cutoffs from the first allocation start, preserving the original two-hour deadline.

The reduced experiment is deployed at `/nfs-share/mh2274/persona-dynamics-20260911` on CaMLSys. Connect through the existing `taranaki` SSH alias. Slurm partition `ampere`, QoS `cls_master`, node `mauao` provides eight A40s. The user explicitly authorized all eight for this two-hour study. Do not run outside Slurm or terminate unrelated jobs.

The launcher is `scripts/camlsys_two_hour.sbatch`. It reserves eight GPUs and 80 CPUs with a two-hour maximum. Generation uses GPUs 0–3 and extraction uses 4–7, sequentially. The node has NVLink pairs 0/1, 2/3, 4/5 and 6/7. Large environment files and model caches live on the local scratch SSD, not quota-limited NFS. The existing shared Python environment is not modified.

Setup downloads the pinned Qwen3-32B checkpoint, published axis and roles, and prepares public data while installing an isolated vLLM 0.9.2 / Transformers 4.53.3 runtime. Exact installed versions are saved in `logs/runtime-versions.json`. CUDA graph capture is disabled (`enforce_eager: true`) to reduce startup time. Request submission batches are 256 so vLLM can schedule the complete gate or E1 workload. These operational settings are frozen in the run manifest.

`scripts/prepare_camlsys.py` resolves and records dataset revisions before generation. The initial broad advice keyword sample failed content review because it admitted coding and fictional prompts. Generation was held while `scripts/screen_camlsys_advice.py` collected stricter candidates. Codex then screened the candidate text and froze 63 public personal/practical advice prompts, giving eight calibration, five E0 and 50 evaluation inputs. This is a convenience sample, not representative coverage or independent human validation. Math uses GSM8K test. No model outputs existed during input selection. The original rejected sample, candidate files, accepted IDs, rules, and hashes are preserved under `data/prepared`; a local copy is in `data/camlsys-prepared`.

The candidate collector saved both complete 100-record candidate files and completion counts, then encountered an Arrow streaming interpreter-shutdown error. Both files were independently parsed, counted, hashed and screened before use. The final study reads frozen JSONL and does not use that streaming path. `scripts/finalize_camlsys_inputs.py` reproduces the accepted advice partition from the saved review manifest. The revised sample deliberately includes personal work, learning, health, legal-procedure, relationship and leisure questions; these broad subtopics are a limitation for domain-specific interpretation.

For subsequent clean deployments, the launcher copies the already-frozen `data/camlsys-prepared` inputs into `data/prepared` when needed. It refuses scientific generation without the saved advice review. Reproducing this run does not require streaming or screening WildChat again.

`scripts/run_camlsys_stages.py` enforces the E0 deadline at minute 35. E1 runs only after a passing gate. It saves E1 results and figures before considering E2. E2 starts only if E1 is saved by minute 70 and a conservative extraction-time estimate fits before minute 100. The original E1 report is retained in `runs/a40-two-hour/e1_snapshot`. Slurm supplies the absolute final time limit, including setup. Failed E0 is reported, not bypassed. Human annotations and interpretation still require review.

Initial job `55665` hit a compute-node `uv` path mismatch before any model generation. It was cancelled after 38 seconds; its setup logs are in `logs/attempt-55665`. Corrected job `55666` reuses its scratch downloads and has a shortened 1h58m limit. Check live status before assuming success:

```bash
ssh taranaki 'squeue -j 55666'
ssh taranaki 'tail -n 30 /nfs-share/mh2274/persona-dynamics-20260911/logs/slurm-55666.out'
ssh taranaki 'tail -n 30 /nfs-share/mh2274/persona-dynamics-20260911/logs/stage-generate-gate.log'
ssh taranaki 'cat /nfs-share/mh2274/persona-dynamics-20260911/logs/stage-timings.json'
```

Cluster verification: 59 standard tests passed, then the real pinned-Qwen tokenizer integration passed separately (60 total). `scripts/camlsys_gpu_check.py` also passed a tiny bf16 Qwen3 forward on an A40 and imported vLLM, with all eight GPUs visible. Runtime is PyTorch 2.7.0+cu126, CUDA 12.6. These checks establish pipeline/runtime behavior; they are not research results. The 32B checkpoint finished downloading in about 2.5 minutes. E0 generation started around minute 15 after input screening completed.

Verified launch milestone: E0 **passed** on the real Qwen3-32B checkpoint (seed 0, three questions, ten roles). Default-minus-role mean centered cosine was 0.10727, with prompt-bootstrap 95% CI [0.10226, 0.11180]; role-rank Spearman correlation was 0.78182. These are calibration checks, not the E1 hypothesis result. Gate generation took 137.2 seconds and the HF calibration/extraction stage took 55.9 seconds. E1 generation began at minute 18.4; job `55666` was still running at minute 19.2. Consult the live logs for subsequent progress and final outcomes.
